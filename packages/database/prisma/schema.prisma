generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Card {
  id       String  @id
  name     String
  oracleId String?
  price    Float?
  foil     Boolean
}
